overwrite: true
schema:
  './src/**/*.ts':
    noRequire: true
documents: null
generates:
  src/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true